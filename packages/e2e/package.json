{
  "name": "@iwer/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pretest": "node -e \"process.exit(require('node:fs').existsSync('../iwer/lib/index.js') ? 0 : 1)\" || pnpm --filter iwer run build",
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/webxr": "^0.5.24",
    "iwer": "workspace:*",
    "vite": "^8.0.10"
  }
}
